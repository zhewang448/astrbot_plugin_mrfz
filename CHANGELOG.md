# 更新日志

## v3.7.3

### 安全修复

- 头像链接校验新增 userinfo 检查，拒绝 `https://prts.wiki@evil.com` 这类绕过主机白名单的写法。
- ZIP 导入改为流式解压并实时累计字节数，不再信任 ZIP 头声明的解压体积，防止高压缩比炸弹。

### 缺陷修复

- 角色下载锁改为 LRU 缓存并限制上限，修复长期运行后锁对象持续累积。
- PRTS 语音记录节点缺少 `data-voice-base` 时不再解析出 `"None"` 字符串。
- 替换、导入与恢复前的备份失败改为记录警告日志，不再静默跳过。
- 操作预览的过期清理加锁，避免并发请求下重复清理。
- 启动时清理上传目录中超过 1 小时的孤儿临时文件，防止进程异常退出后残留。

### 重构

- 新增 `constants.py` 与 `config.py`，集中管理版本号、各类限额、重试参数、语言与语音资源映射。
- 插件配置改由 `PluginConfig` 承载，替换散落的 `config.get()` 调用。
- 补全 `main.py`、`renderer.py`、`data_source.py` 的类型提示。
- 列表渲染数据构建拆分为 5 个独立方法。
- PRTS 主机白名单校验抽为 `_is_trusted_prts_url()`，地址改用统一的 URL 模板常量。
- 新增 `.gitignore`，并将 `__pycache__` 从版本管理中移除。

## v3.7.2

- 支持直接使用皮肤展示名调用语音，例如 `/mrfz 超新星` 会自动解析到维什戴尔的“超新星”皮肤。
- 皮肤名称重复时不再自动猜测，会列出完整皮肤引用供用户选择。

## v3.7.1

- 批量删除、ZIP 覆盖导入和 PRTS 重新下载新增执行预览与二次确认。
- 预览会统计新增、覆盖、相同跳过、待补、损坏重取及预计体积。
- 预览后文件状态变化会拒绝执行，预览令牌过期或取消时会清理临时文件。

## v3.7.0

- 新增 AstrBot Plugin Page“语音档案控制台”，要求 AstrBot `>= 4.26.0`。
- 支持语音试听、单条/整包导出、WAV 替换、ZIP 批量导入和后台 PRTS 任务。
- 新增索引重扫、完整性检查、异常隔离、快捷绑定管理与操作审计。
- 删除操作进入可恢复回收站；替换和恢复覆盖前会自动备份。
- 皮肤基础回退语音标记来源并禁止从皮肤档案误删。

## v3.6.0

- 全面升级多皮肤索引，列表直接显示 `W皮肤 · 恍惚` 等真实皮肤名称。
- 支持 `/mrfz W皮肤[恍惚] 问候 中文` 精确播放；多套皮肤未指定名称时会列出选项，不再自动选择第一套。
- 自定义绑定会固定到具体皮肤，避免后续新增皮肤后指向发生变化。
- 语音索引升级至 v4，记录稳定皮肤资源 ID、展示名称、实际目录、各语言 PRTS voice key 和资源编号映射版本。
- 修正语音资源的非连续编号映射；旧版错位缓存会在首次播放、绑定或手动下载时按角色和语言重新下载，确认 404 的旧文件会移入隔离目录。
- 首次启动会迁移旧版皮肤目录；只校验旧目录实际存在的语言和条目，不要求每套皮肤具备六种语言或固定数量的语音。
- 皮肤语音包缺少个别条目时，播放可回退到同语言的角色基础语音；迁移删除校验仍要求具名皮肤文件完整覆盖旧目录。
- 网络或写入失败时会保留旧目录和重映射标记，并于下次启动重试。
- 重做固定尺寸的明日方舟语音帮助终端页面。

## v3.5.1

- 新增普通用户自动下载开关，默认开启。
- 区分 PRTS 403、404、429、5xx、网络错误和页面结构变化。
- 帮助与列表图片改为固定文件原子覆盖，不再无限累积。
- 修复皮肤列表分类和聚合项重复显示，兼容旧版皮肤目录。
- 校验本地 WAV 与头像内容；损坏 WAV 会在重新下载前移入隔离目录。
- 同步 AstrBot 元数据、帮助说明和依赖。

## v3.5.0

- 增加真实语音索引、多皮肤隔离、下载重试与路径安全校验。
- 重做 PRTS 终端风格渲染器，并修复透明头像色块与右上角布局。

## v3.4.1

- **新增**：自定义语音显示语言。
- **优化**：增加运行效率。

## v3.4.0

- **新增**：模糊匹配功能，输错名字也能智能识别。
- **新增**：自定义语音绑定功能 (`/mrfz_bind`)，数据持久化保存。
- **优化**：重构渲染模块，列表图片包含干员头像，样式升级为 Dark Mode 风格。
- **优化**：代码结构分离为 `Data` / `Renderer` / `Main`，提升维护性。
- **修复**：Linux 环境下字体路径导致的崩溃问题（优先读取插件目录字体）。

## v3.3.7

- 修复了在部分场景下字体失效的问题。

## v3.3.3

- 支持全语言（英/韩/意）。
- 优化下载逻辑。
